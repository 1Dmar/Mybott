# خطة تطوير New Bot Wow 🚀

## 1. تنظيف الكود (تمت البداية)
- إزالة `events1/` و `*.jsy` و `gencode.` وغيرها من الملفات المكررة.
- توحيد نظام الـ Status (يوجد حالياً نظامان متداخلان في `ready.js` و `cronManager.js`).

## 2. الأنظمة الجديدة المقترحة
### أ. نظام المراقبة المتقدم (Live Monitoring)
- تحسين `generateStatusImage` ليدعم قوالب أكثر ديناميكية.
- إضافة إحصائيات حية (TPS, RAM) إذا كان الـ API متاحاً.
- ربط الرسوم البيانية (`PlayerHistory`) بالداشبورد.

### ب. نظام التحكم بالكونسول (Remote Console)
- إضافة واجهة في الداشبورد لإرسال أوامر مباشرة للسيرفر.
- عرض سجلات الكونسول (Logs) في الوقت الفعلي.

### ج. نظام التفاعل (Engagement)
- **Economy:** عملة خاصة بالبوت مرتبطة بنشاط اللاعبين في ماين كرافت.
- **Leaderboards:** لوحات شرف متقدمة (أكثر اللاعبين نشاطاً، أغنى اللاعبين).
- **Auto-Notifications:** إرسال تنبيهات عند دخول لاعبين مشهورين أو وصول السيرفر لعدد معين.

## 3. التكامل مع الداشبورد
- إضافة تبويب "Minecraft Management" في الداشبورد.
- تمكين/تعطيل الأنظمة الجديدة عبر تبويب "Modules".

## 4. الملاحظات التقنية
- توحيد موديلات البيانات (`Server` vs `MinecraftConfig`).
- تحسين معالجة الأخطاء في `interactionCreate.js`.
